The <code><strong>append()</strong></code> method of the <a href="/en-US/docs/Web/API/FormData"><code>FormData</code></a> interface appends a new value onto an existing key inside a <code>FormData</code> object, or adds the key if it does not already exist.