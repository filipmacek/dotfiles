The <strong><code>AbortSignal</code></strong> interface represents a signal object that allows you to communicate with a DOM request (such as a Fetch) and abort it if required via an <a href="/en-US/docs/Web/API/AbortController"><code>AbortController</code></a> object.